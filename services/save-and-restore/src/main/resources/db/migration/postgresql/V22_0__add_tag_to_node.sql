CREATE TABLE tag (
  id SERIAL PRIMARY KEY,
  snapshot_id VARCHAR(100) NOT NULL REFERENCES node(unique_id) ON DELETE CASCADE,
  name VARCHAR(100) NOT NULL,
  comment VARCHAR(1000),
  created TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  username VARCHAR(30)
);
