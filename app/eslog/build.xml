<project default="app-eslog">
  <import file="../../dependencies/ant_settings.xml"/>

  <target name="app-eslog" depends="compile-app">
    <jar destfile="${build}/app-eslog-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
</project>
