<project default="app-display-navigator">
  <import file="../../../dependencies/ant_settings.xml"/>

  <target name="app-display-navigator" depends="compile-app">
    <jar destfile="${build}/app-display-navigator-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
</project>
