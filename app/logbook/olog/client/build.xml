<project default="app-olog">
  <import file="../../../dependencies/ant_settings.xml"/>

  <target name="app-olog" depends="compile-app">
    <jar destfile="${build}/olog-client-${version}.jar">
      <fileset dir="${classes}"/>
    </jar>
  </target>
</project>