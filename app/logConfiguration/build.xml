<project default="app-logConfiguration">
  <import file="../../dependencies/ant_settings.xml"/>

  <target name="app-logConfiguration" depends="compile-app">
    <jar destfile="${build}/app-logConfiguration-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
</project>
