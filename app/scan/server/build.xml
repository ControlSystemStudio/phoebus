<project default="app-scan-server">
  <import file="../../../dependencies/ant_settings.xml"/>

  <target name="app-scan-server">
    <mkdir dir="${classes}"/>
    <javac destdir="${classes}">
      <src path="${src}"/>
      <src path="${test}"/>
      <classpath>
        <path refid="app-classpath"/>
        <pathelement path="../model/${build}/app-scan-model-${version}.jar"/>
      </classpath>
    </javac>
  	
    <jar destfile="${build}/app-scan-server-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
	
</project>