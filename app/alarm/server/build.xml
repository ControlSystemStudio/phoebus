<project default="app-alarm-server">
  <import file="../../../dependencies/ant_settings.xml"/>

  <target name="app-alarm-server">
    <mkdir dir="${classes}"/>
    <javac destdir="${classes}">
      <src path="${src}"/>
      <classpath>
        <path refid="app-classpath"/>
        <pathelement path="../model/${build}/app-alarm-model-${version}.jar"/>
      </classpath>
    </javac>
  	
    <jar destfile="${build}/app-alarm-server-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
	
</project>
