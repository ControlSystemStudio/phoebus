<project default="module-email">
  <import file="../../dependencies/ant_settings.xml"/>

  <target name="module-email" depends="compile-app">
    <jar destfile="${build}/email-${version}.jar">
      <fileset dir="${classes}"/>
      <fileset dir="${src}" includes="**/*.fxml"/>
      <fileset dir="${resources}"/>
    </jar>
  </target>
</project>